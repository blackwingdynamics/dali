[package]
name = "dali-boards"
version = "0.1.0-alpha.3"
edition = "2024"
publish = true
description = "Public hardware-neutral contracts for Dali OS board backends"
license = "MIT OR Apache-2.0"

[dependencies]
dali-kernel-api = { path = "../dali-kernel-api" }

[features]
storage-write = ["dali-kernel-api/storage-write"]

[lints]
workspace = true

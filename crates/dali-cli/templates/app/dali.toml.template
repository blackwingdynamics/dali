[application]
name = "{{ application_name }}"
version = "0.1.0"
sdk = "dali"

[build]
target_profile = "{{ target_profile }}"
abi_version = {{ abi_version }}
profile = "dev"
entry_symbol = "amiran_entry"
entry_offset = 0
# Optional manifest-owned slot name; format version 3 supports non-zero slots.
